---
description:项目编码规则
globs:
alwaysApply: true
---

# 🔹 项目类型
- 本项目使用原生 HTML + CSS + JavaScript，不允许使用框架（如 React、Vue、Angular）。

# 🔹 HTML 规范
- 必须使用语义化标签，保持完整结构（doctype, html, head, body）。
- 图片必须包含 alt 属性。
- 表单必须使用 label 标签。
- 禁止在 HTML 中写大量 JS，统一放入独立的 js 文件。
- 禁止使用内联样式，CSS 必须外链。

# 🔹 CSS 规范
- 统一使用 class 选择器，禁止使用 id 选择器。
- 样式命名采用 BEM 规范（block__element--modifier）。
- 禁止使用 !important，除非有明确注释说明。
- 优先使用 flex / grid 实现布局。
- CSS 文件放在 css/ 目录，必须外链引入。

# 🔹 JS 规范
- 统一使用 const / let，禁止使用 var。
- 所有文件开启 'use strict'。
- DOM 操作必须通过 querySelector / querySelectorAll。
- 事件绑定使用 addEventListener，不允许写在 HTML 标签上。
- 函数必须有明确命名，禁止随处写匿名函数。
- 模块化代码必须拆分文件（支持时使用 type="module"）。

# 🔹 代码风格
- 缩进统一 2 空格。
- JS 语句必须加分号。
- 每行不超过 100 字符。
- 统一使用中文回答。

# 🔹 开发边界规则
- 只实现用户明确要求的内容，禁止顺手增加未请求功能。
- 修改前必须先阅读相关文件，禁止凭猜测改代码。
- 优先使用最小改动解决问题，避免无关重构和大范围扩散修改。
- 修复 bug 时必须先定位根因，再实施修改，不能用兜底逻辑掩盖问题。
- 如果确实需要重构，必须保证范围仅与当前需求直接相关，并说明必要性。

# 🔹 验证规则
- 每次改动完成后，必须说明验证方式与验证结果；未实际执行的验证不能伪造。
- 提交前保证在 Chrome / Firefox / Edge 下运行正常，至少说明本次兼容性检查范围。
- 工具类页面应覆盖空输入、非法输入、超长输入、长结果区等边界场景验证。

# 🔹 可访问性与交互规则
- 所有交互控件必须可通过键盘访问，焦点状态需清晰可见。
- 不允许只依赖颜色表达成功、失败、警告等状态。
- 复制、导出、保存、提交、失败等关键交互必须给出明确反馈。
- 占位符不能替代表单标签，错误提示需清楚说明问题。

# 🔹 响应式与兼容规则
- 页面必须兼容桌面端与常见移动端宽度，避免内容溢出和关键按钮不可用。
- 优先使用兼容性稳定的 Web API；如使用兼容性一般的能力，需确认目标浏览器表现。
- 涉及滚动、复制、下载、选择区、预览切换等功能时，要关注 Chrome、Firefox、Edge 的行为差异。

# 🔹 模块与目录规则
- 当单个 JS 文件或函数过长且已影响当前开发效率时，应在与当前需求直接相关的范围内拆分。
- DOM 操作、业务逻辑、工具函数应尽量按职责分开，避免全部堆在单文件中。
- HTML 放在 `ui/`，CSS 放在 `css/`，JS 放在 `js/`，文档记录放在对应目录下的 `qa.md`。

# 🔹 对话记录规范
- **必须记录**：每次重要对话后，在相关目录下更新 `qa.md` 文件，注意最新记录放在 `qa.md` 文档底部。
- **记录内容**：
  - 问题描述和背景
  - 解决方案和思路
  - 主要代码变更
  - 遇到的问题和解决方法
  - 测试验证方法与结果
  - 后续优化建议
- **文件位置**：各个一级功能目录下的 `qa.md` 文件。
- **记录格式**：使用标准模板，包含时间戳、问题分类、解决方案等。
- **维护方式**：定期整理和归档，保持记录的可读性和实用性。

# 🔹 变更说明规范
- 每次完成改动时，需要明确说明“改了什么、为什么改、影响范围、如何验证”。
- 如果本次改动只涉及配置、规则或文档，也要说明其用途和影响范围。
