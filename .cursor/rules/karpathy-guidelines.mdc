---
description: 受 Karpathy 启发的编码约束（与 CLAUDE.md 一致）：编码前思考、简洁优先、精准修改、目标驱动执行。
globs:
alwaysApply: true
---

# 编码约束（Karpathy 四原则）

与项目根 `CLAUDE.md` 保持一致。倾向谨慎优先于速度；琐碎改动自行判断。

## 1. 编码前思考
不要假设，不要隐藏困惑，呈现权衡。不确定就问；有歧义摆出多种解释；有更简单的办法就提异议；困惑时停下来请求澄清。

## 2. 简洁优先
用最少的代码解决问题。不加要求之外的功能/抽象/灵活性；不为不可能的场景写错误处理；200 行能写成 50 行就重写。检验：资深工程师会嫌它过度复杂吗？

## 3. 精准修改
只碰必须碰的。不顺手改进相邻代码/注释/格式，不重构没坏的东西，匹配现有风格。无关死代码只提不删；只清理自己改动产生的孤儿。检验：每行改动都能追溯到请求。

## 4. 目标驱动执行
定义可验证的成功标准并循环验证：「写重现 bug 的测试→让它通过」「重构前后测试都通过」。多步任务先列「步骤→验证」计划。
