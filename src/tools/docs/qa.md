# Tools工具集 - 对话记录

# Tools工具集 - 对话记录

## 📅 2026-04-11 - Markdown 编辑工具改为单文件自包含页面

### 🔍 问题描述
- **背景**：用户需要 `markdown-editor.html` 便于传播，要求页面本身包含完整逻辑。
- **现象**：之前新增的独立页面仍依赖项目内 `js` 和 `css` 文件，不符合单文件传播诉求。
- **影响**：离开当前项目目录后页面无法独立运行。

### 💡 解决思路
- **分析过程**：确认用户目标是“只保留一个 html 文件即可使用”。
- **解决策略**：将样式、界面结构与核心交互逻辑全部内联到 `markdown-editor.html` 中。
- **技术选择**：保留外部 CDN 依赖，仅去除对项目内本地文件的依赖。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **实现内容**：
  - 内联页面全部 CSS
  - 内联 Markdown 编辑器全部核心 JS 逻辑
  - 保留上传、实时预览、导出 MD、导出 HTML、主题切换、快捷插入、Mermaid 渲染等能力
- **变更说明**：
  - 页面不再依赖项目内其他 `css` 或 `js` 文件
  - 复制该 HTML 文件后仍可单独使用

### ⚡ 技术要点
- **核心逻辑**：单文件内完成编辑区、预览区、文件读取、导出与主题切换。
- **注意事项**：当前仍依赖外部 CDN，若要彻底离线分发，还需继续将第三方库进一步内嵌。
- **传播方式**：现在可以直接单独分发 `markdown-editor.html`。

### ✅ 测试验证
- **测试方法**：检查页面是否还引用项目内本地资源路径。
- **测试结果**：页面已去除项目内本地 `css/js` 依赖，仅保留单文件结构与外部 CDN 资源。
- **性能影响**：无额外项目耦合，页面体积略有增加但更便于分发。

### 🚀 后续优化
- **已知问题**：如果目标环境无法访问 CDN，则 Mermaid / Markdown 解析等第三方能力会受影响。
- **改进方向**：如需彻底离线传播，可继续做“全离线单 HTML 版”。
- **相关任务**：可按需要继续压缩页面体积或补充更多编辑快捷能力。

---

## 📅 2026-04-11 - Markdown 工具新增单独预览模式

### 🔍 问题描述
- **背景**：用户希望 Markdown 工具支持单独查看渲染后的文档内容。
- **现象**：原页面只有编辑区和预览区并排模式，无法专注查看最终渲染效果。
- **影响**：阅读长文档时视觉干扰较多，不利于纯预览场景使用。

### 💡 解决思路
- **分析过程**：基于现有单文件页面结构，最合适的做法是在原工具上增加“预览模式”切换，而不是新开页面。
- **解决策略**：新增预览模式按钮，切换后隐藏编辑区域，仅保留渲染结果区域。
- **技术选择**：通过增加状态变量和样式类控制布局切换，保持现有渲染逻辑不变。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **新增内容**：
  - 工具栏增加“预览模式”按钮
  - 新增 `previewOnly` 状态
  - 新增 `applyPreviewMode()` 用于切换页面布局
  - 预览模式下隐藏编辑面板，并让预览内容单独占满可视区域
- **变更说明**：
  - 不影响原有编辑与实时预览能力
  - 可在编辑模式与单独预览模式之间来回切换

### ✅ 测试验证
- **测试方法**：静态检查按钮事件、状态切换逻辑与预览布局样式。
- **测试结果**：切换预览模式时，页面会隐藏编辑区并单独展示渲染后的 Markdown 内容。
- **性能影响**：无明显性能影响，仅增加少量状态控制逻辑。

### 🚀 后续优化
- **已知问题**：当前预览模式仍保留顶部工具栏，若需要更沉浸阅读可继续增加“纯净阅读模式”。
- **改进方向**：可继续增加“返回编辑”“打印预览”“目录导航”等能力。
- **相关任务**：后续可结合 URL 参数，让页面默认以预览模式打开。

---

## 📅 2026-04-11 - Markdown 工具新增 PDF 导出功能

### 🔍 问题描述
- **背景**：用户希望 Markdown 工具支持将文档直接导出为 PDF。
- **现象**：当前仅支持导出 Markdown 和 HTML，缺少常用的 PDF 输出能力。
- **影响**：用户无法直接生成适合分享和打印的 PDF 文件。

### 💡 解决思路
- **分析过程**：单文件页面中已有完整的 Markdown 编辑和预览逻辑，适合直接在页面内补充 PDF 导出入口。
- **解决策略**：新增 PDF 导出按钮，并通过 `jsPDF` 将当前 Markdown 文本内容分页写入 PDF 文件。
- **技术选择**：使用 CDN 引入 `jsPDF`，保持单文件结构不依赖项目内其他脚本。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **新增内容**：
  - 工具栏增加“导出 PDF”按钮
  - 引入 `jsPDF` 依赖
  - 新增 `exportPdf()` 导出方法
- **变更说明**：
  - 支持直接导出 `document.pdf`
  - 保持原有 MD / HTML 导出功能不变

### ✅ 测试验证
- **测试方法**：静态检查按钮事件、PDF 依赖引入和导出逻辑。
- **测试结果**：页面已具备 PDF 导出入口和相应导出逻辑。
- **性能影响**：增加一个轻量 PDF 依赖，请求体积略有上升。

### 🚀 后续优化
- **已知问题**：当前 PDF 导出以文本分页为主，不会完整保留 HTML 样式效果。
- **改进方向**：如后续需要更高保真导出，可继续结合画布截图或打印样式方案。
- **相关任务**：可后续增加“仅导出预览区”或“打印为 PDF”能力。

---

## 📅 2026-04-11 - Markdown 高保真预览区 PDF 导出修复

### 🔍 问题描述
- **背景**：用户反馈 PDF 导出结果出现乱码，并明确需要高保真且仅导出预览区。
- **现象**：原先的 PDF 导出采用文本写入方式，中文容易乱码，且无法保留预览区实际渲染样式。
- **影响**：导出的 PDF 既无法正确显示内容，也不符合预览区导出需求。

### 💡 解决思路
- **分析过程**：文本写入 PDF 依赖字体支持，浏览器端默认字体无法可靠覆盖中文场景；同时文本导出也无法保留 Markdown 渲染样式。
- **解决策略**：改为将预览区渲染结果截图后分页写入 PDF，只导出预览区内容。
- **技术选择**：使用 `html2canvas` + `jsPDF` 组合实现高保真导出。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **修复内容**：
  - 引入 `html2canvas`
  - 重写 `exportPdf()`，改为截图预览区再分页写入 PDF
  - 导出前自动切换到预览模式，保证只输出预览区内容
- **变更说明**：
  - 解决中文乱码问题
  - PDF 导出结果更接近页面预览效果
  - 导出目标限定为预览区，不再混入编辑区内容

### ✅ 测试验证
- **测试方法**：静态检查依赖引入、预览区截图逻辑和分页写入流程。
- **测试结果**：页面已具备高保真预览区 PDF 导出实现。
- **性能影响**：导出时会增加一次截图和分页处理开销，但仅在导出动作时发生。

### 🚀 后续优化
- **已知问题**：超长文档导出时截图会占用更多内存和时间。
- **改进方向**：后续可增加导出质量选项和分页边距设置。
- **相关任务**：如需进一步优化，可增加“仅导出当前可见主题”或“打印版 PDF”模式。

---

## 📅 2026-04-11 - Markdown PDF 分页截断与尾页压缩修复

### 🔍 问题描述
- **背景**：用户在使用高保真 PDF 导出时，发现分页效果不理想。
- **现象**：分页时内容会被硬切开，且最后一页因为高度计算方式不合理出现压缩。
- **影响**：导出的 PDF 阅读体验差，尤其对表格、代码块和段落块影响明显。

### 💡 解决思路
- **分析过程**：之前按固定像素高度硬切 canvas，导致分页点可能落在内容块中间，同时最后一页仍沿用统一画布高度，造成视觉压缩。
- **解决策略**：基于预览区块级元素位置计算更自然的分页点，并为每一页按真实切片高度生成独立画布。
- **技术选择**：继续使用 `html2canvas` + `jsPDF`，但优化分页算法和尾页切片逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **修复内容**：
  - 导出时先复制预览区到离屏容器，再统一截图
  - 根据块级元素 `offsetTop / offsetHeight` 计算更合理的分页边界
  - 每页使用独立高度画布，避免最后一页被拉伸或压缩
- **变更说明**：
  - 分页更加自然
  - 尾页内容按真实高度导出，不再压缩
  - 保持仅导出预览区和高保真效果

### ✅ 测试验证
- **测试方法**：静态检查分页切片与页面生成逻辑。
- **测试结果**：分页逻辑已改为按内容块优先分页，尾页采用真实高度输出。
- **性能影响**：分页计算略有增加，但只在导出时发生。

### 🚀 后续优化
- **已知问题**：极端超长的单个大块元素仍可能跨页。
- **改进方向**：后续可继续增加针对表格、代码块、Mermaid 图的专用分页策略。
- **相关任务**：如需要可继续做“避免标题单独落页”的排版优化。

---

## 📅 2026-04-11 - 工具集 Markdown 支持高保真 PDF 导出与智能分页

### 🔍 问题描述
- **背景**：用户希望工具集入口 `src/tools/ui/index.html` 中的 Markdown 工具也支持 PDF 导出，并进一步优化分页体验。
- **现象**：工具集版 Markdown 工具此前没有 PDF 导出入口，也缺少针对标题、代码块、表格、Mermaid 图的分页保护。
- **影响**：两个入口能力不一致，且导出 PDF 时页面排版质量不足。

### 💡 解决思路
- **分析过程**：工具集版实际由 `src/tools/js/tools/markdown-editor.js` 渲染，且 `index.html` 负责加载依赖，因此需要同时改入口脚本加载与工具内部实现。
- **解决策略**：给工具集版补充 PDF 依赖和导出按钮，并同步高保真预览区导出能力，再在分页逻辑中增加内容类型保护规则。
- **技术选择**：继续使用 `html2canvas` + `jsPDF`，通过离屏预览容器和分页规则控制导出结果。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/index.html`
  - `src/tools/js/tools/markdown-editor.js`
  - `src/tools/css/markdown-editor.css`
- **新增/修复内容**：
  - `index.html` 增加 `jsPDF` 依赖
  - Markdown 工具控制区增加“导出 PDF”按钮
  - 新增 `exportPdf()`、`decorateExportPreview()`、`computePdfBreakpoints()` 等导出逻辑
  - 增加标题防孤页、代码块尽量整块分页、表格分页优化、Mermaid 图单独分页保护规则
  - 增加导出专用离屏容器样式
- **变更说明**：
  - 工具集入口现已支持仅导出预览区的高保真 PDF
  - 分页更智能，更接近正式文档排版效果

### ✅ 测试验证
- **测试方法**：静态检查入口依赖、按钮绑定、导出逻辑和分页规则。
- **测试结果**：工具集版 Markdown 已接入 PDF 导出能力，并具备分页保护逻辑。
- **性能影响**：导出时会增加截图和分页计算开销，但仅在导出动作发生时生效。

### 🚀 后续优化
- **已知问题**：超长单个表格或单个超高 Mermaid 图仍可能因自身高度超过一页而跨页。
- **改进方向**：后续可继续增加针对超大元素的缩放策略或拆分页策略。
- **相关任务**：如需要可继续统一单文件版与工具集版的分页规则实现细节。

---

## 📅 2026-04-11 - 工具集与单文件版 PDF 导出逻辑对齐修复

### 🔍 问题描述
- **背景**：用户发现 `index.html` 入口中的 Markdown PDF 导出逻辑与单文件版 `src/tools/ui/markdown-editor.html` 不一致。
- **现象**：工具集版使用了不同的导出宽度和容器边距策略，导致 Mermaid 图表等内容在 PDF 中显示不全。
- **影响**：两个入口导出效果不一致，且工具集版的图表可视区域被裁切。

### 💡 解决思路
- **分析过程**：对比发现单文件版导出容器采用更贴近预览区原始尺寸的宽度和无额外 padding 的截图方式，而工具集版在离屏容器上额外增加了更大宽度与边距。
- **解决策略**：将工具集版 `exportPdf()` 对齐到单文件版的核心逻辑，统一导出宽度、离屏容器边距和截图前等待策略。
- **技术选择**：继续保留现有高保真截图分页方案，只修正与单文件版不一致的实现细节。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/markdown-editor.js`
- **修复内容**：
  - 工具集版 PDF 导出宽度调整为与单文件版一致的计算逻辑
  - 去除离屏导出容器上的额外内边距
  - 增加导出前的额外短暂等待，给 Mermaid 图表更充分的布局时间
  - 统一分页缩放基准，避免图表内容被裁切
- **变更说明**：
  - `index.html` 入口的 PDF 导出效果与单文件版趋于一致
  - Mermaid 图表在工具集版导出时显示更完整

### ✅ 测试验证
- **测试方法**：静态对比两套 `exportPdf()` 实现，并检查导出宽度、截图区域和分页基准。
- **测试结果**：工具集版 PDF 导出逻辑已与单文件版对齐，图表裁切风险降低。
- **性能影响**：仅增加极短等待时间，对整体体验影响很小。

### 🚀 后续优化
- **已知问题**：若 Mermaid 图本身超宽，仍可能需要进一步做缩放适配。
- **改进方向**：后续可增加导出前自动缩放 Mermaid SVG 到页宽范围。
- **相关任务**：如需要，可继续把单文件版与工具集版共用一套 PDF 导出核心实现。

---

## 📅 2026-04-11 - PDF 导出逻辑完全对齐与单文件预览按钮修复

### 🔍 问题描述
- **背景**：用户要求 `markdown-editor.js` 直接复用单文件版 `markdown-editor.html` 的 PDF 导出逻辑，同时修复单文件版预览模式下顶部仍显示编辑相关按钮的问题。
- **现象**：工具集版此前在导出逻辑上仍保留了独立实现，单文件版预览模式也没有彻底隐藏编辑类控件。
- **影响**：导出效果仍可能不一致，单文件版预览模式不够纯净。

### 💡 解决思路
- **分析过程**：需要严格按单文件版当前 `exportPdf()` 实现回填到工具集版，避免继续保留偏差；同时在单文件版直接补充预览态隐藏规则。
- **解决策略**：覆盖工具集版 `exportPdf()` 为单文件版同款实现，并通过 `.app--preview` 补充隐藏按钮与状态栏规则。
- **技术选择**：不再做额外抽象，直接对齐现有实现，确保行为一致。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/markdown-editor.js`
  - `src/tools/ui/markdown-editor.html`
- **修复内容**：
  - `markdown-editor.js` 的 `exportPdf()` 完全按单文件版逻辑重写
  - 单文件版预览模式隐藏新建、上传、保存、导出、主题、快捷按钮、字号与状态栏
- **变更说明**：
  - 工具集入口与单文件版 PDF 导出行为保持一致
  - 单文件版预览模式只保留阅读相关最小界面

### ✅ 测试验证
- **测试方法**：静态对比 `exportPdf()` 代码与预览态样式规则。
- **测试结果**：工具集版导出逻辑已与单文件版对齐，单文件版预览模式隐藏规则已补齐。
- **性能影响**：无额外性能负担，仅沿用既有导出流程。

### 🚀 后续优化
- **已知问题**：如后续继续改单文件版导出逻辑，工具集版仍需要同步。
- **改进方向**：后续可提取共用导出模块，避免双份逻辑再次漂移。
- **相关任务**：如需要，可继续统一单双入口的预览模式按钮文案与样式状态。

---

### 🔍 问题描述
- **背景**：用户发现 `index.html` 入口中的 Markdown PDF 导出逻辑与单文件版 `src/tools/ui/markdown-editor.html` 不一致。
- **现象**：工具集版使用了不同的导出宽度和容器边距策略，导致 Mermaid 图表等内容在 PDF 中显示不全。
- **影响**：两个入口导出效果不一致，且工具集版的图表可视区域被裁切。

### 💡 解决思路
- **分析过程**：对比发现单文件版导出容器采用更贴近预览区原始尺寸的宽度和无额外 padding 的截图方式，而工具集版在离屏容器上额外增加了更大宽度与边距。
- **解决策略**：将工具集版 `exportPdf()` 对齐到单文件版的核心逻辑，统一导出宽度、离屏容器边距和截图前等待策略。
- **技术选择**：继续保留现有高保真截图分页方案，只修正与单文件版不一致的实现细节。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/markdown-editor.js`
- **修复内容**：
  - 工具集版 PDF 导出宽度调整为与单文件版一致的计算逻辑
  - 去除离屏导出容器上的额外内边距
  - 增加导出前的额外短暂等待，给 Mermaid 图表更充分的布局时间
  - 统一分页缩放基准，避免图表内容被裁切
- **变更说明**：
  - `index.html` 入口的 PDF 导出效果与单文件版趋于一致
  - Mermaid 图表在工具集版导出时显示更完整

### ✅ 测试验证
- **测试方法**：静态对比两套 `exportPdf()` 实现，并检查导出宽度、截图区域和分页基准。
- **测试结果**：工具集版 PDF 导出逻辑已与单文件版对齐，图表裁切风险降低。
- **性能影响**：仅增加极短等待时间，对整体体验影响很小。

### 🚀 后续优化
- **已知问题**：若 Mermaid 图本身超宽，仍可能需要进一步做缩放适配。
- **改进方向**：后续可增加导出前自动缩放 Mermaid SVG 到页宽范围。
- **相关任务**：如需要，可继续把单文件版与工具集版共用一套 PDF 导出核心实现。

---

### 🔍 问题描述
- **背景**：用户希望工具集入口 `src/tools/ui/index.html` 中的 Markdown 工具也支持 PDF 导出，并进一步优化分页体验。
- **现象**：工具集版 Markdown 工具此前没有 PDF 导出入口，也缺少针对标题、代码块、表格、Mermaid 图的分页保护。
- **影响**：两个入口能力不一致，且导出 PDF 时页面排版质量不足。

### 💡 解决思路
- **分析过程**：工具集版实际由 `src/tools/js/tools/markdown-editor.js` 渲染，且 `index.html` 负责加载依赖，因此需要同时改入口脚本加载与工具内部实现。
- **解决策略**：给工具集版补充 PDF 依赖和导出按钮，并同步高保真预览区导出能力，再在分页逻辑中增加内容类型保护规则。
- **技术选择**：继续使用 `html2canvas` + `jsPDF`，通过离屏预览容器和分页规则控制导出结果。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/index.html`
  - `src/tools/js/tools/markdown-editor.js`
  - `src/tools/css/markdown-editor.css`
- **新增/修复内容**：
  - `index.html` 增加 `jsPDF` 依赖
  - Markdown 工具控制区增加“导出 PDF”按钮
  - 新增 `exportPdf()`、`decorateExportPreview()`、`computePdfBreakpoints()` 等导出逻辑
  - 增加标题防孤页、代码块尽量整块分页、表格分页优化、Mermaid 图单独分页保护规则
  - 增加导出专用离屏容器样式
- **变更说明**：
  - 工具集入口现已支持仅导出预览区的高保真 PDF
  - 分页更智能，更接近正式文档排版效果

### ✅ 测试验证
- **测试方法**：静态检查入口依赖、按钮绑定、导出逻辑和分页规则。
- **测试结果**：工具集版 Markdown 已接入 PDF 导出能力，并具备分页保护逻辑。
- **性能影响**：导出时会增加截图和分页计算开销，但仅在导出动作发生时生效。

### 🚀 后续优化
- **已知问题**：超长单个表格或单个超高 Mermaid 图仍可能因自身高度超过一页而跨页。
- **改进方向**：后续可继续增加针对超大元素的缩放策略或拆分页策略。
- **相关任务**：如需要可继续统一单文件版与工具集版的分页规则实现细节。

---

### 🔍 问题描述
- **背景**：用户在使用高保真 PDF 导出时，发现分页效果不理想。
- **现象**：分页时内容会被硬切开，且最后一页因为高度计算方式不合理出现压缩。
- **影响**：导出的 PDF 阅读体验差，尤其对表格、代码块和段落块影响明显。

### 💡 解决思路
- **分析过程**：之前按固定像素高度硬切 canvas，导致分页点可能落在内容块中间，同时最后一页仍沿用统一画布高度，造成视觉压缩。
- **解决策略**：基于预览区块级元素位置计算更自然的分页点，并为每一页按真实切片高度生成独立画布。
- **技术选择**：继续使用 `html2canvas` + `jsPDF`，但优化分页算法和尾页切片逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **修复内容**：
  - 导出时先复制预览区到离屏容器，再统一截图
  - 根据块级元素 `offsetTop / offsetHeight` 计算更合理的分页边界
  - 每页使用独立高度画布，避免最后一页被拉伸或压缩
- **变更说明**：
  - 分页更加自然
  - 尾页内容按真实高度导出，不再压缩
  - 保持仅导出预览区和高保真效果

### ✅ 测试验证
- **测试方法**：静态检查分页切片与页面生成逻辑。
- **测试结果**：分页逻辑已改为按内容块优先分页，尾页采用真实高度输出。
- **性能影响**：分页计算略有增加，但只在导出时发生。

### 🚀 后续优化
- **已知问题**：极端超长的单个大块元素仍可能跨页。
- **改进方向**：后续可继续增加针对表格、代码块、Mermaid 图的专用分页策略。
- **相关任务**：如需要可继续做“避免标题单独落页”的排版优化。

---

### 🔍 问题描述
- **背景**：用户反馈 PDF 导出结果出现乱码，并明确需要高保真且仅导出预览区。
- **现象**：原先的 PDF 导出采用文本写入方式，中文容易乱码，且无法保留预览区实际渲染样式。
- **影响**：导出的 PDF 既无法正确显示内容，也不符合预览区导出需求。

### 💡 解决思路
- **分析过程**：文本写入 PDF 依赖字体支持，浏览器端默认字体无法可靠覆盖中文场景；同时文本导出也无法保留 Markdown 渲染样式。
- **解决策略**：改为将预览区渲染结果截图后分页写入 PDF，只导出预览区内容。
- **技术选择**：使用 `html2canvas` + `jsPDF` 组合实现高保真导出。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **修复内容**：
  - 引入 `html2canvas`
  - 重写 `exportPdf()`，改为截图预览区再分页写入 PDF
  - 导出前自动切换到预览模式，保证只输出预览区内容
- **变更说明**：
  - 解决中文乱码问题
  - PDF 导出结果更接近页面预览效果
  - 导出目标限定为预览区，不再混入编辑区内容

### ✅ 测试验证
- **测试方法**：静态检查依赖引入、预览区截图逻辑和分页写入流程。
- **测试结果**：页面已具备高保真预览区 PDF 导出实现。
- **性能影响**：导出时会增加一次截图和分页处理开销，但仅在导出动作时发生。

### 🚀 后续优化
- **已知问题**：超长文档导出时截图会占用更多内存和时间。
- **改进方向**：后续可增加导出质量选项和分页边距设置。
- **相关任务**：如需进一步优化，可增加“仅导出当前可见主题”或“打印版 PDF”模式。

---

### 🔍 问题描述
- **背景**：用户希望 Markdown 工具支持将文档直接导出为 PDF。
- **现象**：当前仅支持导出 Markdown 和 HTML，缺少常用的 PDF 输出能力。
- **影响**：用户无法直接生成适合分享和打印的 PDF 文件。

### 💡 解决思路
- **分析过程**：单文件页面中已有完整的 Markdown 编辑和预览逻辑，适合直接在页面内补充 PDF 导出入口。
- **解决策略**：新增 PDF 导出按钮，并通过 `jsPDF` 将当前 Markdown 文本内容分页写入 PDF 文件。
- **技术选择**：使用 CDN 引入 `jsPDF`，保持单文件结构不依赖项目内其他脚本。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **新增内容**：
  - 工具栏增加“导出 PDF”按钮
  - 引入 `jsPDF` 依赖
  - 新增 `exportPdf()` 导出方法
- **变更说明**：
  - 支持直接导出 `document.pdf`
  - 保持原有 MD / HTML 导出功能不变

### ✅ 测试验证
- **测试方法**：静态检查按钮事件、PDF 依赖引入和导出逻辑。
- **测试结果**：页面已具备 PDF 导出入口和相应导出逻辑。
- **性能影响**：增加一个轻量 PDF 依赖，请求体积略有上升。

### 🚀 后续优化
- **已知问题**：当前 PDF 导出以文本分页为主，不会完整保留 HTML 样式效果。
- **改进方向**：如后续需要更高保真导出，可继续结合画布截图或打印样式方案。
- **相关任务**：可后续增加“仅导出预览区”或“打印为 PDF”能力。

---

### 🔍 问题描述
- **背景**：用户希望 Markdown 工具支持单独查看渲染后的文档内容。
- **现象**：原页面只有编辑区和预览区并排模式，无法专注查看最终渲染效果。
- **影响**：阅读长文档时视觉干扰较多，不利于纯预览场景使用。

### 💡 解决思路
- **分析过程**：基于现有单文件页面结构，最合适的做法是在原工具上增加“预览模式”切换，而不是新开页面。
- **解决策略**：新增预览模式按钮，切换后隐藏编辑区域，仅保留渲染结果区域。
- **技术选择**：通过增加状态变量和样式类控制布局切换，保持现有渲染逻辑不变。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **新增内容**：
  - 工具栏增加“预览模式”按钮
  - 新增 `previewOnly` 状态
  - 新增 `applyPreviewMode()` 用于切换页面布局
  - 预览模式下隐藏编辑面板，并让预览内容单独占满可视区域
- **变更说明**：
  - 不影响原有编辑与实时预览能力
  - 可在编辑模式与单独预览模式之间来回切换

### ✅ 测试验证
- **测试方法**：静态检查按钮事件、状态切换逻辑与预览布局样式。
- **测试结果**：切换预览模式时，页面会隐藏编辑区并单独展示渲染后的 Markdown 内容。
- **性能影响**：无明显性能影响，仅增加少量状态控制逻辑。

### 🚀 后续优化
- **已知问题**：当前预览模式仍保留顶部工具栏，若需要更沉浸阅读可继续增加“纯净阅读模式”。
- **改进方向**：可继续增加“返回编辑”“打印预览”“目录导航”等能力。
- **相关任务**：后续可结合 URL 参数，让页面默认以预览模式打开。

---

## 📅 2026-04-11 - 工具集内 Markdown 工具同步预览模式

### 🔍 问题描述
- **背景**：用户希望 `index.html` 中集成的 Markdown 工具也支持预览模式。
- **现象**：单文件版已支持预览模式，但工具集内部弹窗版 Markdown 工具还没有对应能力。
- **影响**：两个入口的交互体验不一致，工具集内无法专注查看渲染结果。

### 💡 解决思路
- **分析过程**：工具集入口实际复用的是 `src/tools/js/tools/markdown-editor.js` 与 `src/tools/css/markdown-editor.css`，因此需要在这两处同步能力。
- **解决策略**：为工具集内 Markdown 工具增加预览模式按钮、状态变量和布局切换样式。
- **技术选择**：延续单文件版方案，通过类名切换隐藏编辑面板，尽量不改动已有渲染逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/markdown-editor.js`
  - `src/tools/css/markdown-editor.css`
- **新增内容**：
  - 控制区新增“预览模式”按钮
  - 增加 `isPreviewOnly` 状态与 `applyPreviewMode()` / `togglePreviewMode()` 方法
  - 预览模式下隐藏编辑区，让预览区单独展示内容
- **变更说明**：
  - 工具集首页打开 Markdown 工具后，也能在弹窗内切换到预览模式
  - 保持原有编辑、导出、主题切换等功能不变

### ✅ 测试验证
- **测试方法**：静态检查工具模板、事件绑定和样式切换逻辑。
- **测试结果**：工具集内 Markdown 工具已具备预览模式切换能力。
- **性能影响**：无明显性能影响，仅增加少量状态与样式控制。

### 🚀 后续优化
- **已知问题**：当前工具集预览模式仍保留顶部控制栏。
- **改进方向**：如有需要可继续增加纯阅读模式，连顶部控制栏一起收起。
- **相关任务**：后续可同步单文件版与工具集版的更多交互细节，保持一致性。

---

## 📅 2026-04-11 - Markdown 预览模式滚动与显示问题修复

### 🔍 问题描述
- **背景**：用户在工具集内使用 Markdown 预览模式时，发现阅读体验异常。
- **现象**：进入预览模式后内容无法正常滚动，同时部分编辑相关控件仍然显示。
- **影响**：导致预览模式无法用于实际阅读，也破坏了“单独查看渲染结果”的目标。

### 💡 解决思路
- **分析过程**：排查发现预览模式下仍保留原本的整体固定布局与局部滚动容器，且工具栏、选项栏没有按场景隐藏。
- **解决策略**：调整预览模式下的容器滚动行为，并补充对编辑相关控件的隐藏规则。
- **技术选择**：继续通过预览模式类名控制样式，不额外改动 Markdown 渲染逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/css/markdown-editor.css`
  - `src/tools/js/tools/markdown-editor.js`
- **修复内容**：
  - 预览模式下允许整体容器滚动
  - 预览面板改为正常内容流，避免被局部滚动限制
  - 隐藏新建、上传、保存、导出、主题、工具栏、字号与分栏选项等非预览控件
  - 预览模式按钮在激活时切换为更明显的按钮状态
- **变更说明**：
  - 预览模式现在更接近真正的阅读态
  - 退出预览后仍保留原有编辑功能和布局

### ✅ 测试验证
- **测试方法**：静态检查预览模式样式规则与按钮状态切换逻辑。
- **测试结果**：预览模式下内容具备滚动能力，且无关编辑控件被隐藏。
- **性能影响**：无明显性能影响。

### 🚀 后续优化
- **已知问题**：顶部仍保留“返回”和“退出预览”两个主操作按钮。
- **改进方向**：如需要可继续增加更极致的纯阅读模式。
- **相关任务**：后续可同步修正单文件版的预览模式细节，保持双入口一致。

---

## 📅 2026-04-11 - 预览模式隐藏状态栏修复

### 🔍 问题描述
- **背景**：用户反馈预览模式下字符数提示栏会随页面一起出现，影响阅读。
- **现象**：底部状态栏在预览模式下仍显示，并随着内容滚动进入阅读区域。
- **影响**：破坏预览模式的纯阅读体验。

### 💡 解决思路
- **分析过程**：预览模式已隐藏部分编辑控件，但状态栏 `.markdown-editor__status` 未纳入隐藏规则。
- **解决策略**：在预览模式下直接隐藏状态栏。
- **技术选择**：仅补充 CSS 规则，不改动现有预览逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/css/markdown-editor.css`
- **修复内容**：
  - 预览模式隐藏 `.markdown-editor__status`
- **变更说明**：
  - 进入预览模式后不再显示字符数、词数、行数等状态提示
  - 退出预览模式后状态栏恢复正常显示

### ✅ 测试验证
- **测试方法**：静态检查预览模式隐藏规则。
- **测试结果**：预览模式下状态栏已被隐藏。
- **性能影响**：无性能影响。

### 🚀 后续优化
- **已知问题**：暂无。
- **改进方向**：可继续统一单文件版与工具集版的预览模式细节。
- **相关任务**：后续如需要，可在阅读模式中增加目录与返回顶部能力。

---

### 🔍 问题描述
- **背景**：用户在工具集内使用 Markdown 预览模式时，发现阅读体验异常。
- **现象**：进入预览模式后内容无法正常滚动，同时部分编辑相关控件仍然显示。
- **影响**：导致预览模式无法用于实际阅读，也破坏了“单独查看渲染结果”的目标。

### 💡 解决思路
- **分析过程**：排查发现预览模式下仍保留原本的整体固定布局与局部滚动容器，且工具栏、选项栏没有按场景隐藏。
- **解决策略**：调整预览模式下的容器滚动行为，并补充对编辑相关控件的隐藏规则。
- **技术选择**：继续通过预览模式类名控制样式，不额外改动 Markdown 渲染逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/css/markdown-editor.css`
  - `src/tools/js/tools/markdown-editor.js`
- **修复内容**：
  - 预览模式下允许整体容器滚动
  - 预览面板改为正常内容流，避免被局部滚动限制
  - 隐藏新建、上传、保存、导出、主题、工具栏、字号与分栏选项等非预览控件
  - 预览模式按钮在激活时切换为更明显的按钮状态
- **变更说明**：
  - 预览模式现在更接近真正的阅读态
  - 退出预览后仍保留原有编辑功能和布局

### ✅ 测试验证
- **测试方法**：静态检查预览模式样式规则与按钮状态切换逻辑。
- **测试结果**：预览模式下内容具备滚动能力，且无关编辑控件被隐藏。
- **性能影响**：无明显性能影响。

### 🚀 后续优化
- **已知问题**：顶部仍保留“返回”和“退出预览”两个主操作按钮。
- **改进方向**：如需要可继续增加更极致的纯阅读模式。
- **相关任务**：后续可同步修正单文件版的预览模式细节，保持双入口一致。

---

### 🔍 问题描述
- **背景**：用户希望 `index.html` 中集成的 Markdown 工具也支持预览模式。
- **现象**：单文件版已支持预览模式，但工具集内部弹窗版 Markdown 工具还没有对应能力。
- **影响**：两个入口的交互体验不一致，工具集内无法专注查看渲染结果。

### 💡 解决思路
- **分析过程**：工具集入口实际复用的是 `src/tools/js/tools/markdown-editor.js` 与 `src/tools/css/markdown-editor.css`，因此需要在这两处同步能力。
- **解决策略**：为工具集内 Markdown 工具增加预览模式按钮、状态变量和布局切换样式。
- **技术选择**：延续单文件版方案，通过类名切换隐藏编辑面板，尽量不改动已有渲染逻辑。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/markdown-editor.js`
  - `src/tools/css/markdown-editor.css`
- **新增内容**：
  - 控制区新增“预览模式”按钮
  - 增加 `isPreviewOnly` 状态与 `applyPreviewMode()` / `togglePreviewMode()` 方法
  - 预览模式下隐藏编辑区，让预览区单独展示内容
- **变更说明**：
  - 工具集首页打开 Markdown 工具后，也能在弹窗内切换到预览模式
  - 保持原有编辑、导出、主题切换等功能不变

### ✅ 测试验证
- **测试方法**：静态检查工具模板、事件绑定和样式切换逻辑。
- **测试结果**：工具集内 Markdown 工具已具备预览模式切换能力。
- **性能影响**：无明显性能影响，仅增加少量状态与样式控制。

### 🚀 后续优化
- **已知问题**：当前工具集预览模式仍保留顶部控制栏。
- **改进方向**：如有需要可继续增加纯阅读模式，连顶部控制栏一起收起。
- **相关任务**：后续可同步单文件版与工具集版的更多交互细节，保持一致性。

---

### 🔍 问题描述
- **背景**：用户希望 Markdown 工具支持单独查看渲染后的文档内容。
- **现象**：原页面只有编辑区和预览区并排模式，无法专注查看最终渲染效果。
- **影响**：阅读长文档时视觉干扰较多，不利于纯预览场景使用。

### 💡 解决思路
- **分析过程**：基于现有单文件页面结构，最合适的做法是在原工具上增加“预览模式”切换，而不是新开页面。
- **解决策略**：新增预览模式按钮，切换后隐藏编辑区域，仅保留渲染结果区域。
- **技术选择**：通过增加状态变量和样式类控制布局切换，保持现有渲染逻辑不变。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **新增内容**：
  - 工具栏增加“预览模式”按钮
  - 新增 `previewOnly` 状态
  - 新增 `applyPreviewMode()` 用于切换页面布局
  - 预览模式下隐藏编辑面板，并让预览内容单独占满可视区域
- **变更说明**：
  - 不影响原有编辑与实时预览能力
  - 可在编辑模式与单独预览模式之间来回切换

### ✅ 测试验证
- **测试方法**：静态检查按钮事件、状态切换逻辑与预览布局样式。
- **测试结果**：切换预览模式时，页面会隐藏编辑区并单独展示渲染后的 Markdown 内容。
- **性能影响**：无明显性能影响，仅增加少量状态控制逻辑。

### 🚀 后续优化
- **已知问题**：当前预览模式仍保留顶部工具栏，若需要更沉浸阅读可继续增加“纯净阅读模式”。
- **改进方向**：可继续增加“返回编辑”“打印预览”“目录导航”等能力。
- **相关任务**：后续可结合 URL 参数，让页面默认以预览模式打开。

---

### 🔍 问题描述
- **背景**：用户需要 `markdown-editor.html` 便于传播，要求页面本身包含完整逻辑。
- **现象**：之前新增的独立页面仍依赖项目内 `js` 和 `css` 文件，不符合单文件传播诉求。
- **影响**：离开当前项目目录后页面无法独立运行。

### 💡 解决思路
- **分析过程**：确认用户目标是“只保留一个 html 文件即可使用”。
- **解决策略**：将样式、界面结构与核心交互逻辑全部内联到 `markdown-editor.html` 中。
- **技术选择**：保留外部 CDN 依赖，仅去除对项目内本地文件的依赖。

### 🔧 代码变更
- **修改文件**：
  - `src/tools/ui/markdown-editor.html`
- **实现内容**：
  - 内联页面全部 CSS
  - 内联 Markdown 编辑器全部核心 JS 逻辑
  - 保留上传、实时预览、导出 MD、导出 HTML、主题切换、快捷插入、Mermaid 渲染等能力
- **变更说明**：
  - 页面不再依赖项目内其他 `css` 或 `js` 文件
  - 复制该 HTML 文件后仍可单独使用

### ⚡ 技术要点
- **核心逻辑**：单文件内完成编辑区、预览区、文件读取、导出与主题切换。
- **注意事项**：当前仍依赖外部 CDN，若要彻底离线分发，还需继续将第三方库进一步内嵌。
- **传播方式**：现在可以直接单独分发 `markdown-editor.html`。

### ✅ 测试验证
- **测试方法**：检查页面是否还引用项目内本地资源路径。
- **测试结果**：页面已去除项目内本地 `css/js` 依赖，仅保留单文件结构与外部 CDN 资源。
- **性能影响**：无额外项目耦合，页面体积略有增加但更便于分发。

### 🚀 后续优化
- **已知问题**：如果目标环境无法访问 CDN，则 Mermaid / Markdown 解析等第三方能力会受影响。
- **改进方向**：如需彻底离线传播，可继续做“全离线单 HTML 版”。
- **相关任务**：可按需要继续压缩页面体积或补充更多编辑快捷能力。

---

## 📅 2026-04-11 - Markdown 编辑工具抽离独立页面

### 🔍 问题描述
- **背景**：用户希望将现有 Markdown 编辑工具单独抽离成一个可独立传播的页面。
- **现象**：当前 Markdown 编辑器依附在工具集主页面弹窗中使用，不便于单独分发。
- **影响**：影响工具的独立访问、传播和复用效率。

### 💡 解决思路
- **分析过程**：
  1. 检查现有工具集入口和 Markdown 编辑器脚本、样式依赖。
  2. 确认可直接复用 `markdown-editor.js` 与 `markdown-editor.css`。
  3. 按“不改动原有代码”的要求，仅新增独立 HTML 页面作为单独入口。
- **解决策略**：创建新的独立 HTML 文件，直接挂载现有 Markdown 编辑器类并初始化。
- **技术选择**：保留原脚本与样式文件，页面中按需引入 `marked`、`DOMPurify`、`highlight.js`、`mermaid` 等依赖。

### 🔧 代码变更
- **新增文件**：
  - `src/tools/ui/markdown-editor.html` - Markdown 编辑工具独立页面入口
- **实现内容**：
  - 引入 `markdown-editor.css`
  - 引入 Markdown 解析、高亮和流程图所需 CDN 依赖
  - 复用 `MarkdownEditorTool` 渲染并初始化页面
- **变更说明**：
  - 未修改原有工具集页面及原有工具逻辑
  - 新页面可直接单独打开使用

### ⚡ 技术要点
- **核心逻辑**：通过独立 HTML 容器挂载已有 `MarkdownEditorTool` 实例。
- **注意事项**：独立页面仍依赖外部 CDN 资源，需在联网环境下完整使用高亮与 Mermaid 渲染能力。
- **兼容方式**：保留原工具集中的注册逻辑，新页面不依赖主应用初始化流程。

### ✅ 测试验证
- **测试方法**：静态检查页面引用路径与初始化逻辑是否完整。
- **测试结果**：新页面具备独立渲染 Markdown 编辑器所需资源与脚本。
- **性能影响**：无额外业务逻辑开销，仅新增独立访问入口。

### 🚀 后续优化
- **已知问题**：页面标题和导出文件名仍沿用现有工具默认配置。
- **改进方向**：如后续需要完全脱离主工程，可再将脚本和样式进一步打包为单文件分发版本。
- **相关任务**：可考虑为独立页面增加工具入口导航或品牌说明。

---

## 📅 2025-09-24 - 目录结构重构

### 🔍 问题描述
- **背景**：统一各模块目录结构，规范化项目组织
- **现象**：Tools工具集模块目录结构不规范，文件散乱分布
- **影响**：影响代码维护和团队协作效率

### 💡 解决思路
- **分析过程**：按照统一规范重新组织目录结构
- **解决策略**：创建标准目录，移动文件到对应位置
- **技术选择**：采用模块化的目录组织方式

### 🔧 代码变更
- **新增目录**：
  - `docs/` - 文档目录
  - `data/input/` - 输入数据目录
  - `data/output/` - 输出数据目录
  - `data/export/` - 导出数据目录
  - `css/` - 样式目录
  - `js/` - JavaScript逻辑目录
  - `ui/assets/images/` - 图片资源目录
- **文件移动**：
  - `qa.md` → `docs/qa.md`
  - `README.md` → `docs/technical.md`
  - `ToolsPrd.md` → `docs/ToolsPrd.md`
  - `tools.html` → `ui/index.html`
  - 更新所有CSS和JS文件引用路径

### ⚡ 技术要点
- **核心逻辑**：按功能分类组织文件，便于维护
- **注意事项**：更新HTML文件中的资源引用路径
- **最佳实践**：分离数据、代码、文档和资源

### ✅ 测试验证
- **测试方法**：验证所有工具功能正常运行
- **测试结果**：所有功能正常，文件路径正确
- **性能影响**：无性能影响，仅文件重组

### 🚀 后续优化
- **已知问题**：暂无
- **改进方向**：可考虑添加配置文件管理
- **相关任务**：完善PRD文档和README

---

# Tools工具集 - 对话记录

## 📅 2024-09-24 - Bug修复

### 🔍 问题描述
- **背景**：用户反馈Web工具集页面一直显示加载状态，无法正常使用
- **现象**：加载动画不消失，页面卡在加载界面，控制台显示工具加载成功但UI未更新
- **影响**：影响所有用户正常使用工具集功能

### 💡 解决思路
- **分析过程**：
  1. 通过控制台日志发现工具加载成功，但UI显示异常
  2. 检查HTML发现加载动画初始就有`loading--hidden`类
  3. 分析CSS发现隐藏样式可能被其他样式覆盖
  4. 确认JavaScript的`hideLoading()`方法执行但效果不明显
- **解决策略**：CSS优先级提升 + JavaScript多重隐藏保障
- **技术选择**：使用`!important`提升CSS优先级，JavaScript设置多种隐藏属性

### 🔧 代码变更
- **修改文件**：
  - `src/tools/css/layout.css` - 添加`!important`确保隐藏样式优先级
  - `src/tools/js/app.js` - 增强`hideLoading()`方法的多重隐藏保障
- **关键代码**：
  ```css
  .loading--hidden {
    display: none !important;  /* 添加 !important 确保优先级 */
  }
  ```
  ```javascript
  // 强制隐藏加载动画 - 多重保障
  loading.classList.add('loading--hidden');
  loading.style.display = 'none';
  loading.style.visibility = 'hidden';
  loading.style.opacity = '0';
  loading.style.pointerEvents = 'none';
  ```

### ⚡ 技术要点
- **核心逻辑**：CSS优先级问题需要通过`!important`解决，JavaScript提供多重保障
- **注意事项**：多重隐藏属性确保在各种情况下都能隐藏元素
- **最佳实践**：JavaScript和CSS双重保障提高UI控制的可靠性

### ✅ 测试验证
- **测试方法**：刷新页面验证加载动画正确隐藏，工具列表正常显示
- **测试结果**：加载动画成功隐藏，页面正常显示工具列表，功能恢复正常
- **性能影响**：无性能影响，仅增加少量CSS和JS代码

### 🚀 后续优化
- **已知问题**：暂无
- **改进方向**：可考虑添加更好的加载状态可视化反馈
- **相关任务**：优化其他页面的加载体验，统一加载状态处理逻辑

---

## 📅 2025-09-24 - 项目规范完善

### 🔍 问题描述
- **背景**：用户希望设置对话记录规则，便于项目维护和知识积累
- **现象**：虽然规则文件中有对话记录要求，但缺乏具体实施规范和模板
- **影响**：影响项目的知识积累、问题追踪和团队协作效率

### 💡 解决思路
- **分析过程**：
  1. 检查现有规则文件，发现第45行已有记录要求但不够详细
  2. 分析项目目录结构，确定需要记录的主要功能模块
  3. 设计标准化的记录格式和内容规范
  4. 创建可复用的模板和示例
- **解决策略**：完善规则定义 + 创建标准模板 + 提供操作指导
- **技术选择**：Markdown格式记录，标准化模板，按功能模块分类

### 🔧 代码变更
- **修改文件**：
  - `.cursor/rules/rules.mdc` - 完善对话记录规范，增加详细要求
- **新增文件**：
  - `docs/qa-template.md` - 标准对话记录模板，包含格式和使用说明
  - `src/tools/qa.md` - 工具集模块的对话记录示例
- **规则更新**：
  ```markdown
  # 🔹 对话记录规范
  - **必须记录**：每次重要对话后，在相关目录下更新 `qa.md` 文件
  - **记录内容**：问题描述、解决方案、代码变更、技术要点、测试验证、后续优化
  - **文件位置**：各个一级功能目录下的 `qa.md` 文件
  - **记录格式**：使用标准模板，包含时间戳、问题分类、解决方案等
  - **维护方式**：定期整理和归档，保持记录的可读性和实用性
  ```

### ⚡ 技术要点
- **核心逻辑**：标准化记录格式，确保信息完整性和可读性
- **注意事项**：及时记录，避免遗漏重要信息；分类管理，便于查找
- **最佳实践**：版本控制管理，团队协作维护，定期整理归档

### ✅ 测试验证
- **测试方法**：创建模板和示例记录，验证格式和内容完整性
- **测试结果**：模板格式清晰，信息结构完整，示例记录详细
- **性能影响**：无性能影响，纯文档记录，不影响应用功能

### 🚀 后续优化
- **已知问题**：需要手动维护记录，可能存在遗漏
- **改进方向**：
  - 可考虑开发自动化记录工具
  - 集成到开发工作流中
  - 添加搜索和标签功能
- **相关任务**：
  - 在其他功能模块创建qa.md文件
  - 定期整理和归档历史记录
  - 培训团队成员使用记录规范

---

## 📅 2024-01-15 - 日程提醒工具需求文档创建

### 🔍 问题描述
- **背景**：用户需要日程提醒工具，支持将事件写入电脑和手机系统日历
- **现象**：现有工具集缺少日程管理功能，用户需要跨平台的日历事件管理
- **影响**：影响用户的时间管理和日程安排效率

### 💡 解决思路
- **分析过程**：
  1. 分析用户需求，确定核心功能范围
  2. 研究现有工具集架构，设计集成方案
  3. 调研日历格式标准，确定技术实现方案
  4. 设计用户界面和交互流程
- **解决策略**：创建详细的产品需求文档，包含功能设计、技术方案、界面设计
- **技术选择**：iCal/VCS格式导出，localStorage本地存储，响应式设计

### 🔧 代码变更
- **新增文件**：
  - `src/tools/docs/calendar-reminder-prd.md` - 日程提醒工具产品需求文档
- **文档内容**：
  - 产品概述和目标用户分析
  - 核心功能需求（基础管理、系统集成、高级功能）
  - 用户界面设计（主界面、表单、导出设置）
  - 技术实现方案（文件结构、类设计、数据模型）
  - 验收标准和开发计划

### ⚡ 技术要点
- **核心功能**：事件CRUD、日历导出、重复规则、提醒设置
- **技术难点**：iCal/VCS格式生成、时区处理、重复规则实现
- **最佳实践**：模块化设计、标准化格式、用户体验优先

### ✅ 测试验证
- **测试方法**：文档内容审查，技术方案可行性分析
- **测试结果**：需求文档完整，技术方案可行，界面设计合理
- **性能影响**：无性能影响，纯文档创建

### 🚀 后续优化
- **已知问题**：需要实际开发验证技术方案
- **改进方向**：
  - 根据开发过程中的问题调整需求
  - 优化用户界面和交互体验
  - 增加更多高级功能
- **相关任务**：
  - 开始日程提醒工具的开发实现
  - 创建技术实现文档
  - 设计详细的UI界面

---

## 📅 2025-01-15 - 日程提醒工具按钮功能修复

### 🔍 问题描述
- **背景**：用户反馈日程提醒工具的单条导出按钮点击无效，无法显示格式选择模态框
- **现象**：点击事件卡片上的📤导出按钮后，控制台显示方法被调用，但模态框不显示
- **影响**：影响用户使用单条导出功能，无法将单个事件导出到日历应用

### 💡 解决思路
- **分析过程**：
  1. 通过控制台日志确认按钮点击事件正常触发
  2. 确认`exportEvent`方法被正确调用
  3. 确认`showSingleExportFormat`方法被调用
  4. 发现模态框被创建但未显示的根本原因：缺少`display: flex`设置
- **解决策略**：在动态创建模态框后立即设置显示样式
- **技术选择**：JavaScript动态设置CSS样式，确保模态框可见

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/calendar-reminder.js` - 修复三个模态框显示问题
- **关键修复**：
  ```javascript
  // 在showSingleExportFormat方法中
  document.body.appendChild(formatModal);
  formatModal.style.display = 'flex';  // 关键修复：设置显示样式
  
  // 在showImportFormatHelp方法中
  document.body.appendChild(importModal);
  importModal.style.display = 'flex';  // 关键修复：设置显示样式
  
  // 在showSettings方法中
  document.body.appendChild(settingsModal);
  settingsModal.style.display = 'flex';  // 关键修复：设置显示样式
  ```
- **新增文件**：
  - `src/tools/modal-fix-test.html` - 专门测试模态框修复效果的测试页面
  - `src/tools/button-fix-summary.md` - 详细的修复总结文档

### ⚡ 技术要点
- **核心问题**：动态创建的DOM元素默认`display: none`，需要手动设置为`flex`才能显示
- **修复范围**：单条导出、导入帮助、设置三个模态框
- **最佳实践**：创建模态框后立即设置显示样式，确保用户能看到

### ✅ 测试验证
- **测试方法**：创建专门的测试页面验证所有模态框功能
- **测试结果**：
  - ✅ 单条导出按钮正常显示格式选择模态框
  - ✅ 导入按钮正常显示导入帮助模态框
  - ✅ 设置按钮正常显示设置模态框
  - ✅ 批量导出按钮正常显示导出设置模态框
- **性能影响**：无性能影响，仅修复显示问题

### 🚀 后续优化
- **已知问题**：无
- **改进方向**：
  - 可考虑统一模态框创建和显示逻辑
  - 可考虑添加模态框动画效果
- **相关任务**：
  - 清理测试文件
  - 更新用户文档

---

## 📅 2025-01-15 - 字符串分割工具功能增强

### 🔍 问题描述
- **背景**：用户需要更强大的字符串处理功能，包括数据清理和数字提取
- **现象**：现有字符串分割工具缺少数据清理功能，无法处理包含空格、空行、换行符的复杂数据
- **影响**：影响用户处理复杂文本数据的效率，无法满足数据清理需求

### 💡 解决思路
- **分析过程**：
  1. 分析用户需求，确定需要的数据清理功能
  2. 设计数据清理选项的UI界面
  3. 实现预处理和数据清理的核心逻辑
  4. 添加数字提取功能
- **解决策略**：增加数据清理选项，实现预处理和清理逻辑，支持数字提取
- **技术选择**：JavaScript正则表达式，DOM操作，数据预处理

### 🔧 代码变更
- **修改文件**：
  - `src/tools/js/tools/string-splitter.js` - 增加数据清理功能
- **新增功能**：
  - 去除首尾空格选项
  - 去除空行选项
  - 去除空单元格选项
  - 去除换行符选项
  - 只提取数字内容选项
- **核心代码**：
  ```javascript
  // 新增数据清理选项
  this.cleanOptions = {
    trimSpaces: false,        // 去除首尾空格
    removeEmptyLines: false,  // 去除空行
    removeEmptyCells: false, // 去除空单元格
    removeLineBreaks: false, // 去除换行符
    extractNumbers: false    // 只提取数字内容
  };
  
  // 数据清理逻辑
  applyDataCleaning() {
    // 去除首尾空格
    if (this.cleanOptions.trimSpaces) {
      processedCell = processedCell.trim();
    }
    
    // 只提取数字内容
    if (this.cleanOptions.extractNumbers) {
      const numbers = processedCell.match(/\d+(?:\.\d+)?/g);
      processedCell = numbers ? numbers.join(' ') : '';
    }
  }
  ```

### ⚡ 技术要点
- **核心功能**：数据预处理、单元格清理、数字提取、空行过滤
- **技术难点**：正则表达式数字匹配、数据清理顺序、UI状态管理
- **最佳实践**：模块化清理逻辑，用户友好的选项界面，完整的状态重置

### ✅ 测试验证
- **测试方法**：使用复杂文本数据测试各种清理选项
- **测试结果**：
  - ✅ 去除首尾空格功能正常
  - ✅ 去除空行功能正常
  - ✅ 去除空单元格功能正常
  - ✅ 去除换行符功能正常
  - ✅ 数字提取功能正常
  - ✅ 选项状态重置功能正常
- **性能影响**：无性能影响，仅增加数据处理逻辑

### 🚀 后续优化
- **已知问题**：无
- **改进方向**：
  - 可考虑添加更多数据清理选项（如去除特殊字符）
  - 可考虑添加数据验证功能
  - 可考虑添加批量处理功能
- **相关任务**：
  - 更新用户文档
  - 测试各种数据场景
  - 优化用户体验

---

## 📅 2024-01-15 - 日程提醒工具开发实现

### 🔍 问题描述
- **背景**：根据需求文档实现日程提醒工具的完整功能
- **现象**：需要创建可用的日程管理工具，支持事件CRUD、日历导出等功能
- **影响**：为用户提供完整的日程管理解决方案

### 💡 解决思路
- **分析过程**：
  1. 创建主工具类CalendarReminderTool
  2. 实现事件CRUD操作（创建、读取、更新、删除）
  3. 实现日历导出功能（iCal/VCS格式）
  4. 设计响应式用户界面
  5. 集成到主应用系统
- **解决策略**：模块化开发，逐步实现各个功能模块
- **技术选择**：原生JavaScript、CSS3、localStorage存储

### 🔧 代码变更
- **新增文件**：
  - `src/tools/js/tools/calendar-reminder.js` - 主工具类（800+行）
  - `src/tools/css/calendar-reminder.css` - 样式文件（500+行）
  - `src/tools/test-calendar.html` - 测试页面
- **修改文件**：
  - `src/tools/js/app.js` - 添加工具注册
  - `src/tools/ui/index.html` - 添加样式和脚本引用
- **核心功能**：
  - 事件CRUD操作（创建、编辑、删除、查看）
  - 搜索和筛选功能（按标题、分类、时间）
  - 日历导出功能（iCal/VCS格式）
  - 响应式用户界面
  - 本地数据存储

### ⚡ 技术要点
- **核心功能**：完整的事件管理系统，支持所有CRUD操作
- **导出功能**：支持iCal和VCS两种标准格式，兼容主流日历应用
- **用户体验**：响应式设计，支持移动端操作
- **数据管理**：localStorage本地存储，支持数据持久化
- **界面设计**：现代化UI，符合项目设计规范

### ✅ 测试验证
- **测试方法**：创建独立测试页面，验证所有功能
- **测试结果**：
  - ✅ 事件创建、编辑、删除功能正常
  - ✅ 搜索和筛选功能正常
  - ✅ 日历导出功能正常（iCal/VCS格式）
  - ✅ 响应式界面适配正常
  - ✅ 本地存储功能正常
- **性能影响**：无性能问题，代码优化良好

### 🚀 后续优化
- **已知问题**：无重大问题，功能完整可用
- **改进方向**：
  - 可考虑添加更多重复规则选项
  - 可考虑添加事件模板功能
  - 可考虑添加数据导入功能
- **相关任务**：
  - 完成集成测试和最终验证
  - 更新项目文档
  - 准备发布版本

---

## 📅 2026-04-11 - Markdown 工具首页无入口

### 🔍 问题描述
- **背景**：在 `src/tools/ui/index.html` 中已引入 `markdown-editor.js` 与样式，期望在工具网格中出现「Markdown编辑器」卡片。
- **现象**：首页不显示该工具入口；控制台可能报脚本 404 或注册失败。
- **影响**：无法从工具集打开 Markdown 编辑器。

### 💡 解决思路
- **原因 1**：`index.html` 引用路径为 `../js/tools/markdown-editor.js`（即 `src/tools/js/tools/`），实际文件只在 `src/js/tools/markdown-editor.js`，脚本未加载。
- **原因 2**：`window.ToolRegistry` 原在 `app.js` 末尾赋值，各工具脚本先于 `app.js` 执行时 `ToolRegistry` 不存在，无法 `register`；加载 `app.js` 后又用空表覆盖，导致从注册表读到的工具列表为空（与「仅有 Markdown 不显示」并存时，表现为新工具永远进不了列表）。

### 🔧 代码变更
- **新增**：`src/tools/js/tools/markdown-editor.js`（与首页引用路径一致，并增加 `'use strict'`）。
- **修改**：`src/tools/js/utils.js` 在文件末尾初始化全局 `ToolRegistry`，保证工具脚本加载时即可注册。
- **修改**：`src/tools/js/app.js` 中仅在 `!window.ToolRegistry` 时创建注册表，避免覆盖已注册工具。
- **删除**：`src/js/tools/markdown-editor.js`（避免双份维护，以后以 `src/tools/js/tools/markdown-editor.js` 为准）。

### ⚡ 技术要点
- 脚本顺序：`utils.js` → 各 `tools/*.js`（内联 `register`）→ `app.js`（`registerTools` 从 `ToolRegistry` 拉取）。

### ✅ 测试验证
- 打开 `src/tools/ui/index.html`，应能看到「Markdown编辑器」卡片；点击后在弹窗中正常编辑与预览。

### 🚀 后续优化
- 可将文件输入框的 `display:none` 改为 BEM 工具类，符合「禁止内联样式」规范。

---

## 📅 2026-04-11 - Markdown 弹窗「工具加载失败」与需求对齐

### 🔍 问题描述
- **现象**：打开 Markdown 编辑器时弹窗显示「工具加载失败」。
- **根因**：marked v4 UMD 全局为对象，应使用 `marked.parse()`，原先用 `marked(str)` 会抛出 `TypeError`；`highlight.js` 主题 CSS 曾误用 `<script>` 引入；样式文件未落在 `src/tools/css/` 导致 404。

### 🔧 代码变更
- `markdown-editor.js`：`runMarkedParse` 优先 `marked.parse`，`init` 不再重复调用 `bindEvents`（与 `app.js` 约定一致）；增加 DOMPurify 过滤、10MB 限制、拖拽上传、工具栏、快捷键、分栏比例、状态栏、友好错误文案等（对齐需求文档）。
- `index.html`：主题样式改为 `<link>`；增加 `dompurify.min.js`。
- `src/tools/css/markdown-editor.css`：补齐工具页引用路径与新增 UI 样式。
- `app.js`：加载失败时展示具体错误信息（转义后插入）。
- `docs/需求文档/markdown工具.md`：更新技术依赖说明（marked.parse、DOMPurify、保存方式）。

### ✅ 测试验证
- 打开工具集首页 → Markdown 编辑器 → 应正常渲染空白编辑区与预览；输入标题/代码块可预览；保存/导出、主题切换可用。

---

## 📅 2026-04-11 - Markdown 工具返回后出现多余弹窗

### 🔍 问题描述
- **背景**：用户从 Markdown 编辑器点击“返回”回到工具首页。
- **现象**：返回操作后页面还会再出现一个浏览器弹窗，影响正常使用。
- **根因**：`goBack()` 直接移除编辑器节点后又调用 `window.history.back()` 作为兜底逻辑，未走当前页面已有的工具弹窗关闭流程，导致返回行为异常并触发额外交互弹窗。

### 💡 解决思路
- 不再手动删除 `.markdown-editor` 节点，也不再调用浏览器历史返回。
- 统一复用主应用现有的弹窗关闭方法，直接关闭 `toolModal`，让页面回到工具列表。

### 🔧 代码变更
- **修改文件**：`src/tools/js/tools/markdown-editor.js`
- **关键修复**：重写 `goBack()`，优先调用 `window.webToolsApp.closeModal()`；若主应用实例不可用，则仅移除弹窗激活状态并恢复页面滚动。

### ⚡ 技术要点
- Markdown 编辑器本身运行在工具详情弹窗内部，“返回”本质上应是关闭当前工具弹窗，而不是页面级历史回退。
- 统一走主应用的关闭逻辑，可以避免重复 DOM 清理和异常弹窗。

### ✅ 测试验证
- 打开首页 → 进入 Markdown 编辑器 → 点击“返回” → 应直接关闭工具弹窗并回到工具列表，不再出现额外弹窗。

### 🚀 后续优化
- 可考虑给工具组件增加统一的“退出当前工具”接口，避免各工具自行处理返回逻辑。

---

## 📅 2026-04-11 - Markdown 编辑器 Mermaid 图表未生效分析

### 🔍 问题描述
- **背景**：页面已在 `index.html` 中引入 Mermaid 脚本，用户希望在 Markdown 编辑器预览区直接显示流程图等图表。
- **现象**：即使输入 Mermaid 语法，预览区也不会渲染成图表，只会当作普通代码块或普通文本显示。
- **根因**：当前 `markdown-editor.js` 只做了 `marked` 解析、HTML 清洗和 `highlight.js` 代码高亮，没有在预览完成后识别 `mermaid` 代码块并调用 Mermaid 进行二次渲染。

### 💡 解决思路
- 保留 Markdown 正常解析流程。
- 在 `updatePreview()` 完成 HTML 注入后，查找预览区内的 Mermaid 代码块。
- 将 Mermaid 代码块替换为 Mermaid 容器，并调用 Mermaid API 执行渲染。

### 🔧 当前使用说明
- **正确输入方式**：必须使用 fenced code block，并把语言标记写成 `mermaid`。
- **示例**：
  ```markdown
  ```mermaid
  graph TD
    A[开始] --> B[输入内容]
    B --> C{是否有效}
    C -->|是| D[渲染图表]
    C -->|否| E[提示错误]
  ```
  ```
- **注意**：当前版本即使这样输入，也不会真正渲染为图表，仍需补上 Mermaid 渲染逻辑。

### ✅ 结论
- `index.html` 已具备 Mermaid 依赖加载条件。
- 真正缺失的是 `src/tools/js/tools/markdown-editor.js` 中的 Mermaid 识别与渲染代码，而不是输入语法本身。

### 🚀 后续优化
- 可在导出 HTML 时一并内嵌 Mermaid 渲染支持，保证导出文件也能展示图表。

---

## 📅 2026-04-11 - caveman 安装失败网络问题修复

### 🔍 问题描述
- **背景**：用户执行 `npx skills add JuliusBrussee/caveman -a cursor` 安装 `caveman` skill。
- **现象**：安装过程中仓库克隆失败，出现 GitHub 连接超时或空响应错误。
- **根因**：本机可解析 `github.com`，但直连 `443` 端口失败；同时系统已存在可用的本地代理 `127.0.0.1:7897`，但 `git` 与 `npm` 未统一使用该代理进行拉取。

### 💡 解决思路
- 先检查 `git`、环境变量、系统代理与本地代理端口状态。
- 确认本地代理可用后，为 `git` 和 `npm` 补充 `http/https proxy` 配置。
- 在安装命令执行时同时注入 `HTTP_PROXY` 与 `HTTPS_PROXY` 环境变量，确保 `npx skills` 拉取 GitHub 仓库时走代理。

### 🔧 排查结果
- **Git 版本**：`git version 2.11.0.windows.1`
- **GitHub 连通性**：`github.com` DNS 解析正常，但直连 `443` 失败。
- **本地代理**：`127.0.0.1:7897` 端口可连通。
- **系统代理线索**：系统代理解析结果指向 `http://127.0.0.1:7897/`。

### 🔧 修复操作
- 设置 Git 全局代理：
  - `git config --global http.proxy http://127.0.0.1:7897`
  - `git config --global https.proxy http://127.0.0.1:7897`
- 设置 npm 代理：
  - `npm config set proxy http://127.0.0.1:7897`
  - `npm config set https-proxy http://127.0.0.1:7897`
- 使用代理环境变量重试安装：
  - `HTTP_PROXY=http://127.0.0.1:7897`
  - `HTTPS_PROXY=http://127.0.0.1:7897`

### ✅ 结果验证
- 安装已成功完成，共安装 5 个 skills：
  - `caveman`
  - `caveman-compress`
  - `caveman-commit`
  - `caveman-help`
  - `caveman-review`
- 安装目标为 Cursor，本地已成功复制到 `.agents/skills/` 对应目录。

### ⚠️ 注意事项
- 当前 `git` 与 `npm` 已保留全局代理配置；如果后续不再使用本地代理，需手动取消相关配置。
- `skills` 输出中提示这些 skills 具有完整 agent 权限，使用前应自行审阅内容。

### 🚀 后续优化
- 可考虑把代理配置同步到常用终端启动脚本，避免后续每次临时注入环境变量。
- 如果本地代理端口会变化，可进一步改成按实际代理工具自动检测。

---

## 📅 2026-04-12 - Cursor skills 识别验证与常用 skills 补充安装

### 🔍 问题描述
- **背景**：用户希望继续完成三件事：验证 Cursor 是否已识别已安装的 skills、补充安装常用 skills、整理一个可复用的 Windows 代理脚本。
- **目标**：确认 `caveman` 系列已在当前项目内被 Cursor 识别，并补齐常见辅助型 skills，同时沉淀代理运维脚本便于后续重复使用。

### 💡 解决思路
- 先通过 `skills list` 的 JSON 输出核实 Cursor 当前可识别的 skills。
- 再从公开 skills 仓库中筛选通用度较高、风险较低的辅助 skills 进行安装。
- 最后在项目内新增批处理脚本，统一封装 Git/npm 代理设置、清理与状态查看能力。

### 🔧 验证结果
- 使用 `npx skills ls -a cursor --json` 验证后，以下 skills 已被 Cursor 正常识别：
  - `caveman`
  - `caveman-commit`
  - `caveman-compress`
  - `caveman-help`
  - `caveman-review`
- 说明当前 `.agents/skills/` 下的项目级安装结果已生效。

### 🔧 新增安装内容
- 新增安装了 3 个常用 skills：
  - `git-commit`
  - `documentation-writer`
  - `prd`
- 安装目标均为 Cursor，且已成功复制到项目目录 `.agents/skills/`。
- 这些 skills 来自 `github/awesome-copilot` 仓库，安装输出中显示为低风险。

### 🧰 代理脚本整理
- **新增文件**：`others/dev-proxy/proxy-setup.bat`
- **支持能力**：
  - `set [host] [port]`：设置 Git、npm 与当前终端代理
  - `unset`：清理 Git、npm 与当前终端代理
  - `status`：查看当前代理配置状态
- **默认代理地址**：`127.0.0.1:7897`

### ✅ 使用示例
- 设置默认代理：`proxy-setup.bat set`
- 指定代理地址：`proxy-setup.bat set 127.0.0.1 7897`
- 查看状态：`proxy-setup.bat status`
- 清理代理：`proxy-setup.bat unset`

### ⚠️ 注意事项
- `proxy-setup.bat` 中设置的环境变量只对当前执行该脚本的终端会话生效；Git 与 npm 的全局配置会持久保留。
- 新安装的 skills 同样具有完整 agent 权限，建议按需使用，并在正式高权限操作前先阅读对应 `SKILL.md`。

### 🚀 后续优化
- 可继续补充一个 PowerShell 版本脚本，支持更友好的参数解析与错误提示。
- 如果后续常用 skill 范围更明确，可再按代码审查、文档生成、需求整理等场景做分类安装。

---

## 📅 2026-04-12 - Markdown 编辑器预览模式新增文档目录

### 🔍 问题描述
- **背景**：用户希望在 Markdown 编辑器的预览模式下显示 `.md` 文档目录，包含各级标题层级，并要求将相同能力同步迁移到 `src/tools/js/tools/markdown-editor.js` 中。
- **目标**：无论是独立页 `src/tools/ui/markdown-editor.html`，还是工具模块 `src/tools/js/tools/markdown-editor.js`，都需要在预览区旁边展示可点击的目录导航。

### 💡 解决思路
- 在预览区外层增加一个两栏结构：左侧目录、右侧正文预览。
- 每次 Markdown 渲染完成后，扫描 `h1` 到 `h6` 标题，自动生成唯一锚点 ID。
- 根据标题层级生成目录列表，点击目录项时平滑滚动到对应标题位置。
- 同步补充深色主题与响应式样式，确保在桌面和窄屏下都可正常显示。

### 🔧 代码变更
- **修改文件**：`src/tools/ui/markdown-editor.html`
  - 新增 `.preview-shell`、`.toc`、`.preview-wrap` 结构。
  - 新增目录生成逻辑：`slugifyHeading()`、`applyHeadingAnchors()`、`renderToc()`。
  - 在 `update()` 中渲染 Markdown 后先生成目录，再继续 Mermaid 渲染。
- **修改文件**：`src/tools/js/tools/markdown-editor.js`
  - 预览面板新增 `markdownToc` 目录容器。
  - 新增实例字段：`tocElement`、`tocHeadings`。
  - 新增方法：`applyHeadingAnchors()`、`slugifyHeading()`、`renderTableOfContents()`、`scrollToHeading()`、`escapeHtml()`。
  - 在 `updatePreview()` 中增加目录生成与标题锚点处理。
- **修改文件**：`src/tools/css/markdown-editor.css`
  - 新增目录容器、层级缩进、深色主题、预览模式布局、响应式适配等样式。

### ✅ 实现效果
- 预览区左侧会显示“文档目录”。
- 支持 `h1` 到 `h6` 各级标题缩进展示。
- 点击目录项会滚动到对应标题位置。
- 没有标题时会显示“当前文档暂无标题”。
- 独立 HTML 页面和工具模块版行为保持一致。

### ⚠️ 注意事项
- 目录基于预览渲染后的标题文本生成，重复标题会自动追加序号，避免锚点冲突。
- 目录点击跳转使用当前预览容器滚动，不依赖浏览器页面级锚点跳转。

### 🚀 后续优化
- 可继续增加“当前阅读位置高亮目录项”功能。
- 可考虑为超长目录增加折叠能力，提升大文档浏览体验。

---

## 📅 2026-04-12 - Markdown 编辑器预览模式目录跳转修复

### 🔍 问题描述
- **背景**：新增目录功能后，用户反馈在预览模式下点击目录项无法跳转到对应标题位置。
- **现象**：目录可正常渲染，但点击目录链接后预览区域不滚动，尤其在“预览模式”下更明显。
- **根因**：原实现直接使用 `heading.offsetTop` 配合局部容器滚动，这种方式在不同滚动容器之间并不稳定；普通模式下滚动容器是预览面板，预览模式下滚动容器变成编辑器根容器，导致点击目录时计算出的滚动位置不准确。

### 💡 解决思路
- 抽离统一的“预览滚动容器”获取逻辑。
- 不再依赖单纯的 `offsetTop`，而是改成基于 `getBoundingClientRect()` 与当前滚动条位置计算相对目标位置。
- 独立页与模块版统一采用相同跳转策略，保证两边行为一致。

### 🔧 代码变更
- **修改文件**：`src/tools/ui/markdown-editor.html`
  - 新增 `getPreviewScrollContainer()` 与 `scrollToHeading()`。
  - 目录点击时统一调用 `scrollToHeading()`，自动根据是否处于预览模式决定滚动容器。
- **修改文件**：`src/tools/js/tools/markdown-editor.js`
  - 新增 `getPreviewScrollContainer()` 方法。
  - 重写 `scrollToHeading()`，改为根据目标标题与滚动容器的矩形位置差计算滚动目标。

### ✅ 修复结果
- 普通编辑预览模式下，点击目录项可以滚动到对应标题。
- 单独预览模式下，点击目录项也能正确跳转。
- 目录跳转逻辑在独立页和工具模块版中保持一致。

### 🚀 后续优化
- 可在滚动时自动高亮当前目录项，提升长文档定位体验。
- 如后续需要，也可支持目录项展开/折叠与返回顶部按钮。

---

## 📅 2026-04-12 - 新增精简版开发 skills 与项目规则补充

### 🔍 问题描述
- **背景**：用户希望按之前确认的“精简版清单”，为项目补充更适合日常编程协作的 skills，并同步增强项目级 rules。
- **目标**：让后续开发在前端调试、可访问性检查、跨浏览器排查、QA 记录、Bug 修复边界控制等方面有更明确的协作约束与能力说明。

### 💡 解决思路
- 先检查当前 `.agents/skills/` 与 `.cursor/rules/` 的现有结构，保持与项目现有组织方式一致。
- 按精简版清单新增 8 个技能目录，每个 skill 提供清晰的适用场景、执行原则与输出要求。
- 统一整理 `rules.mdc`，将原有 HTML / CSS / JS 规范保留，并补充最小改动、验证、可访问性、响应式、目录归属、变更说明等规则。

### 🔧 代码变更
- **新增目录与文件**：
  - `.agents/skills/frontend-debugger/SKILL.md`
  - `.agents/skills/ui-ux-review/SKILL.md`
  - `.agents/skills/accessibility-checker/SKILL.md`
  - `.agents/skills/cross-browser-debug/SKILL.md`
  - `.agents/skills/qa-recorder/SKILL.md`
  - `.agents/skills/bugfix-first/SKILL.md`
  - `.agents/skills/refactor-guard/SKILL.md`
  - `.agents/skills/git-diff-explainer/SKILL.md`
- **修改文件**：
  - `.cursor/rules/rules.mdc`
- **实现内容**：
  - 新增面向原生前端项目的调试、评审、记录、边界控制类 skills。
  - 规则文件中补充开发边界、改后验证、可访问性、响应式与目录结构要求。
  - 清理原规则文件顶部重复 frontmatter，统一为单一规则入口。

### ✅ 测试验证
- **验证方法**：检查新增 skills 目录结构与 `SKILL.md` 文件是否创建成功，并重新读取 `rules.mdc` 确认内容已更新。
- **验证结果**：8 个新增 skills 已写入项目 `.agents/skills/` 目录；`rules.mdc` 已更新为扩展后的规则版本。
- **说明**：本次主要为配置与协作规范补充，未涉及页面运行逻辑变更。

### ⚠️ 注意事项
- 新增 skills 目前为项目内本地说明型 skills，后续如需进一步细化，可按具体使用习惯继续增强。
- `rules.mdc` 的模块拆分规则保留为“影响当前开发效率时再拆分”，避免对现有长文件做无关强制重构。

### 🚀 后续优化
- 可继续补充更细的测试清单型 skill，例如工具页边界场景测试模板。
- 如后续项目目录继续扩展，可把规则按“前端规范 / 协作规范 / 文档规范”拆成多个规则文件。

---

## 📅 2026-04-12 - 补充 Android 开发相关 skills 与 rules

### 🔍 问题描述
- **背景**：用户补充说明本职是 Android 应用开发，希望在现有项目级协作能力基础上，再增加一组更贴近 Android 日常开发的 skills 与 rules。
- **目标**：覆盖 Android 调试、UI 状态评审、性能检查、架构边界、发布检查、测试规划等常见工作场景，并在统一规则文件中加入 Android 相关开发约束。

### 💡 解决思路
- 先复用现有 `.agents/skills/` 目录结构，为 Android 场景新增独立 skill 目录，保持和前端 skills 同一层级。
- skills 设计上优先覆盖最常见的六类高频任务：调试、状态评审、性能、架构约束、发布检查、测试规划。
- rules 侧不单独新建文件，而是在现有 `.cursor/rules/rules.mdc` 中补充 Android 开发规范、验证规则、兼容性和可访问性要求，避免规则入口分散。

### 🔧 代码变更
- **新增目录与文件**：
  - `.agents/skills/android-debugger/SKILL.md`
  - `.agents/skills/android-ui-state-review/SKILL.md`
  - `.agents/skills/android-performance-checker/SKILL.md`
  - `.agents/skills/android-architecture-guard/SKILL.md`
  - `.agents/skills/android-release-checker/SKILL.md`
  - `.agents/skills/android-test-planner/SKILL.md`
- **修改文件**：
  - `.cursor/rules/rules.mdc`
- **实现内容**：
  - 新增 Android 生命周期、状态流、协程、发布、测试相关 skills。
  - 规则文件中补充 Kotlin 优先、生命周期根因定位、线程边界、状态恢复、权限与发布配置边界等要求。
  - 在验证规则中加入 Android 设备 / 系统版本覆盖说明，以及旋转屏、前后台、权限拒绝、弱网等场景要求。

### ✅ 测试验证
- **验证方法**：检查 6 个 Android skill 目录与 `SKILL.md` 是否成功创建，并重新读取 `rules.mdc` 确认 Android 规范段落已写入。
- **验证结果**：Android 相关 skills 已全部写入 `.agents/skills/`；`rules.mdc` 已补充 Android 开发规范、验证规则和兼容性要求。
- **说明**：本次仍为协作规范与说明文件扩展，未涉及 Android 业务代码运行验证。

### ⚠️ 注意事项
- 当前项目本身仍是原生 Web 工具项目，因此 Android 相关内容主要服务于通用开发协作，不会改变现有前端目录结构。
- Android 规则采用“补充型”写法，与原有前端规则共存；后续若 Android 相关工作量继续增大，可再拆分为独立规则文件。

### 🚀 后续优化
- 可继续补充更偏工程化的 Android skills，例如 `android-code-reviewer`、`android-crash-analyzer`、`android-gradle-config-checker`。
- 若后续你希望更贴合个人开发习惯，我也可以再帮你补一版“Jetpack Compose / MVVM / Room / Retrofit / Hilt 定制规则集”。

